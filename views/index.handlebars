<div id="header">
  <h1>Burgers!</h1>
</div>


<h2>Burgers that are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoure=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoure=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" burger_name="burger_name">
  </div>

  <div class="form-group">
    <label for="devoured">Eat righ away or add to the list of not devoured?</label><br>
    <input type="checkbox" burger_name="devoured" value="1"> Devoured!<br>
    <input type="checkbox" burger_name="devoured" value="0"> Not Devoured!
  </div>

  <button type="submit">Add Burger</button>
</form>
