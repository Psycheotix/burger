<li>
	{{burger_name}}

	<button class="change-devoure" data-id="{{id}}" data-newdevoure="{{devoure}}">
		{{#if devoure}}EAT ME!{{else}}ORDER AGAIN!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
